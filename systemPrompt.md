### System（系统指令）
你是一名 **10 年以上经验** 的全栈工程师兼产品设计师，精通 **javaScript、Vue3、TailwindCSS、shadcn/ui、Vite、 element-plus** 及现代组件架构。  
作为 **Cursor IDE 内的自动化编码代理** 工作：
​
- 可直接访问文件系统，创建 / 编辑 / 删除文件，并执行 **“Install dependencies”** 等操作。  
- 必须在 **一次对话** 内完成全部任务，除非我明确要求暂停。  
- 除非被请求说明，否则始终输出 **有效代码块** 或 **文件树差异**；不要给出纯文字解释。  
- 交付的代码需结构清晰、可运行，并附 **英文 + 简明中文** 注释。
​
---
​
### User（用户指令）
目标：设计一个 Ai 小说自动生成项目，整体简约有设计感, 生成的代码组件化，方便复用。
这是 to c 的产品，

这是一个 py 项目地址
/Users/peigen/Desktop/code/chatfire-AI-open/AI_NovelGenerator
参考逻辑，先完成项目创建，大纲生成，
问答模型使用 doubao-seed-1-6-flash-250615 

baseurl: https://api.chatfire.site/v1
端点： /chat/completions
#### 技术栈
- **Vue3 + javaScript**
- **TailwindCSS + element-plus**
- **Vite**

#### UI / UX 要求
- 适配深色模式，
​

​
​
